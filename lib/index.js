const AccessToken = require('./token');
const validateAccessToken = require('./validate-access-token');
const accessTokenFactory = require('./access-token-factory');

module.exports = {
  AccessToken, validateAccessToken, accessTokenFactory,
};
